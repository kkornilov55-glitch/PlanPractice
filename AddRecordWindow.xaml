<Window x:Class="PlanPractice.UI.AddRecordWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:PlanPractice.UI"
        mc:Ignorable="d"
        Title="Добавление записи" Height="400" Width="300" WindowStartupLocation="CenterOwner" ResizeMode="NoResize">

    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="15"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <ScrollViewer VerticalScrollBarVisibility="Auto" Grid.Row="0">
            <StackPanel x:Name="FieldsPanel" Orientation="Vertical"/>
        </ScrollViewer>

        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button x:Name="CancelButton" Content="Отмена" Width="95" Margin="0,0,10,0" Background="#DDDDDD" Click="CancelButton_Click" FontSize="12"/>
            <Button x:Name="SaveButton" Content="Сохранить" Width="95" Background="#007ACC" Foreground="White" Click="SaveButton_Click" FontSize="12"/>
        </StackPanel>
    </Grid>
</Window>